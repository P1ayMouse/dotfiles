# dotfiles

My shell configuration (zsh) managed with `chezmoi`.

## What is included
- `.zshrc` (zinit + plugins)

## Setup on a clean macOS profile

1) Install Homebrew (if not installed):
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
